# Test dependencies for IRIS Gate MCP integration tests

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0  # For parallel test execution

# For capturing logs and output
pytest-caplog>=0.7.0

# MCP dependencies (must match main requirements.txt)
chromadb>=0.4.0
GitPython>=3.1.40
tqdm>=4.65.0

# Standard library equivalents needed for testing
python-dotenv>=1.0.0
